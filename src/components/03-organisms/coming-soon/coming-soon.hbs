{{#exists coming-soon data='coming-soon'}}
  <div class="coming-soon">
    {{#if image }}{{> @image}}{{/if}}
    <p class="title">{{ title }}</p>
    <p class="text">{{ text }}</p>
  </div>
{{/exists}}

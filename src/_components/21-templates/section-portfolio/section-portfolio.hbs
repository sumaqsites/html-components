{{#if sectionPortfolio }}
  {{#exists sectionPortfolio }}
    <section id="id" class="section-portfolio">
      <h2 class="title">{{ title }}</h2>
      {{> @gallery }}
    </section>
  {{/exists }}
{{else}}
  {{> @data-not-found data='sectionPortfolio'}}
{{/if}}

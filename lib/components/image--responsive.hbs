{{#exists image data="image"}}
  <figure class="image responsive">
    <img
      class="lazyload"
      src="{{imageBlurred name}}"
      data-srcset="{{imageSource name}}"
      data-sizes="auto"
      alt="{{description}}"
    />
  </figure>
{{/exists}}

{{#with image }}
<figure class="image responsive">
  <img
    class="lazyload"
    src="{{blured name}}"
    data-srcset="{{sourceSet name}}"
    data-sizes="auto"
    alt="{{ description }}"
  />
</figure>
{{else }}
  {{> @data-not-found data='image'}}
{{/with}}
